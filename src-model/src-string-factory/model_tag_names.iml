(* Imandra Inc. copyright 2021 *)
[@@@program]
[@@@import "../src-types/model_tags.iml"]
open Model_tags;;


let tag_to_message (x)  =
    (match x with
        | "D" -> (Some M_Msg_NewOrderSingle_Tag)
        | _ -> None
    )
;;

let tag_to_rg (x)  =
    (match x with
        | _ -> None
    )
;;

let tag_to_field (x)  =
    (match x with
        | "44" -> (Some M_Field_Price_Tag)
        | _ -> None
    )
;;

[@@@logic]
