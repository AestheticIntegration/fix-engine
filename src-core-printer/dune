(library
  (name        core_printer)
  (public_name core_printer)
  (wrapped  false)
  (flags (:standard -open Imandra_prelude))
  (libraries imandra-prelude core_pp core_time_defaults_pp core yojson)
)
