(library
  (name        protocol_pp)
  (public_name protocol_pp)
  (wrapped  false)
  (flags (:standard -open Imandra_prelude))
  (libraries imandra-prelude yojson protocol_exts_pp protocol fix_engine core_utils)
)
