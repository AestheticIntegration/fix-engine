(* Aesthetic Integration copyright 2019 *)

let get_resend_message (msg)  =
    (match msg with
        | m -> m
    )
;;

let get_normal_message (msg)  =
    (match msg with
        | m -> None
    )
;;
