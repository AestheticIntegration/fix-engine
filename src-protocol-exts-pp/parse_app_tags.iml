(* Aesthetic Integration copyright 2019 *)

let parse_app_msg_tag (tag)  =
    (match tag with
        | _ -> (None)
    )
;;

let parse_app_field_tag (tag)  =
    (match tag with
        | _ -> (None)
    )
;;
